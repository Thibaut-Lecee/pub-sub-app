apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: pubsub-keyvault # needs to be unique per namespace
spec:
  provider: azure
  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "true" # Set to true for using managed identity
    objects: |
      array:
        - |
          objectName: tlecee-rabbitmq-username
          objectType: secret
        - |
          objectName: tlecee-rabbitmq-password
          objectType: secret
        - |
          objectName: tlecee-mssql-password
          objectType: secret
        - |
          objectName: tlecee-mssql-connectionstring
          objectType: secret
  secretObjects:
    - secretName: mssql-password
      type: Opaque
      data:
        - objectName: tlecee-mssql-password # name of the mounted content to sync. this could be the object name or object alias
          key: mssql-password
    - secretName: mssql-connectionstring
      type: Opaque
      data:
        - objectName: tlecee-mssql-connectionstring # name of the mounted content to sync. this could be the object name or object alias
          key: mssql-connectionstring
    - secretName: rabbit-password
      type: Opaque
      data:
        - objectName: tlecee-rabbitmq-password # name of the mounted content to sync. this could be the object name or object alias
          key: rabbit-password
    - secretName: rabbit-username
      type: Opaque
      data:
        - objectName: tlecee-rabbitmq-username # name of the mounted content to sync. this could be the object name or object alias
          key: rabbit-username
